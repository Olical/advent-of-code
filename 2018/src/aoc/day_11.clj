(ns aoc.day-11
  (:require [clojure.test :as t]
            [aoc.core :as aoc]))

(t/deftest day-11-a
  (t/testing "input"
    (t/is (= 0 0))))

(t/deftest day-11-b
  (t/testing "input"
    (t/is (= 0 0))))

(def grid-serial 9110)
